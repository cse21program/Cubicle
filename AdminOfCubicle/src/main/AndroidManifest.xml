<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adminofcubicle">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AdminOfCubicle"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PhoneAuthenticationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MessageActivity" />
        <activity android:name=".ImageViewerActivity" />
        <activity android:name=".EventListActivity" />
        <activity android:name=".EventsMangeActivity" />
        <activity android:name=".LikeAndCommentActivity" />
        <activity android:name=".ClubsProfileActivity" />
        <activity android:name=".StudentProfileActivity" />
        <activity
            android:name=".SettingActivity"
            android:parentActivityName=".ProfileActivity" />
        <activity
            android:name=".FeedbackActivity"
            android:parentActivityName=".ProfileActivity" />
        <activity
            android:name=".DevelopersActivity"
            android:parentActivityName=".ProfileActivity" />
        <activity android:name=".ProfileActivity" />
    </application>

</manifest>